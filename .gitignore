# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.env
.venv
env/
venv/
ENV/

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg

# Unit test / coverage
.pytest_cache/
htmlcov/
.coverage
*.cover

# Jupyter Notebooks
.ipynb_checkpoints

# dotenv / secrets
.env
.env.local
*.env

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project-specific
# Clarius SDK binaries + TLS material (must stay at repo root; not committed)
libcast.dylib
pyclariuscast.so
pri.pem
tls.crt

# Large / regenerable data (kept locally, not committed)
data/
media/

# Note: handeye.json (calibration result) and outputs/ are intentionally tracked.