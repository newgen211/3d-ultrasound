# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.env
.venv
env/
venv/
ENV/

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg

# Unit test / coverage
.pytest_cache/
htmlcov/
.coverage
*.cover

# Jupyter Notebooks
.ipynb_checkpoints

# dotenv / secrets
.env
.env.local
*.env

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project-specific
# Clarius SDK binaries + TLS material (must stay at repo root; not committed)
libcast.dylib
pyclariuscast.so
pri.pem
tls.crt

# Large / regenerable data (kept locally, not committed)
data/
media/
runs/
yolo_ds/
*.ply
*.npz
sim_handoff.zip

# Audit: frames are regenerable, TRUTH IS NOT — carve it out
audit/*
!audit/audit_truth.json
!audit/audit_manifest.json

# Pose/exec logs are data, but launch + teach files are work products — keep them
pose_logs/exec_*
!pose_logs/sweep_teach_*.jsonl
!pose_logs/shifted_*.jsonl

# Note: handeye.json, calibration jsons, exemplars.json, outputs/ intentionally tracked.